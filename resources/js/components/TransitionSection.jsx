import styles from "./../../css/styles/transitionSection.module.scss"
export default function TransitionSection({className}){1
    return (
        <div className={`${styles.transitonColor} ${className}` }></div>
    )
}